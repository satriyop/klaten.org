# Club has a required Sport; Spectra follows Sport, not a flag

A Club stores Sport with the same ids as Hub Listings. Nightly Spectra fan-out targets Clubs where Sport is `renang`. A `spectra_enabled` boolean was rejected: it duplicates Sport and reopens “BMSC-only by accident.”
